const plus_one = `
<h2>plus_one</h2>

<h3>問題文</h3>
<p>配列が与えられ、その末尾に1を追加する関数を作成せよ。</p>

<h3>制約</h3>
<p>関数の引数には配列を受け取り、返り値に配列を返すこととする</p>
<pre>
@param {number[]}
@return {number[]}
</pre>

<h3>入出力例</h3>
<pre>
<code>
[1, 2, 3] // param
[1, 2, 4] // return

[1, 2, 3, 4, 5, 10] // param
[1, 2, 3, 4, 5, 11] // return
</code>
</pre>
`;

export default plus_one;
