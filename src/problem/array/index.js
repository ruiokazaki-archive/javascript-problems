import plus_one from './plus_one';
import maximum_product_of_three_numbers from './maximum_product_of_three_numbers';
import reverse_array from './reverse_array';
import exclude_negative_numbers from './exclude_negative_numbers';

const array = {
  plus_one,
  maximum_product_of_three_numbers,
  reverse_array,
  exclude_negative_numbers,
};
export default array;
