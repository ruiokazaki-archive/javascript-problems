{
  "name": "js-sandbox",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build; git add dist/; git commit -m 'deploy'; git push; push-dir --dir=dist --branch=gh-pages --force"
  },
  "devDependencies": {
    "push-dir": "^0.4.1",
    "vite": "^2.9.9"
  }
}
