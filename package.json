{
  "name": "js-sandbox",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "push-dir": "^0.4.1",
    "vite": "^2.9.9"
  }
}
